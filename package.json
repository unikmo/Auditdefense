{
  "name": "auditdefend-demo",
  "version": "0.4.0",
  "private": true,
  "description": "Non-production redacted pilot for AuditDefend — The Audit Defense Forensic Platform.",
  "scripts": {
    "dev": "python3 -m http.server 4173 --directory public",
    "check": "node --check public/app.js && node --check public/firebase-client.js && node --check public/auth.js && node --check public/verify.js && node --check public/attorney.js && node --check public/provider.js && node --check public/attorneys.js",
    "build": "npm run check"
  }
}
